import { constants, utils } from 'ethers';
import { expect } from 'chai';


describe('YToken: Core', () => {

    it('should throw if the initialized with the same version', async () => {

    });
});
